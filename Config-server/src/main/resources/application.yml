---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/koshasheth24/ConfigData
          searchPaths: ConfigData
        native:
          searchLocations: classpath:offline-repository/
server:
  port: 8001